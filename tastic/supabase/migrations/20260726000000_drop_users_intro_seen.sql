-- ============================================================
-- users.intro_seen 제거 — 기능 가이드(온보딩 투어) 노출을 계정 단위에서
-- 기기 로컬(AsyncStorage) 단위로 되돌린다.
--
-- 이제 인트로는 로그인 이전, "이 기기에서 앱을 처음 실행했는지"로 판단하므로
-- 계정 컬럼이 필요 없다. 20260723000000 마이그레이션을 원상복구한다.
-- ============================================================

alter table public.users
  drop column if exists intro_seen;
